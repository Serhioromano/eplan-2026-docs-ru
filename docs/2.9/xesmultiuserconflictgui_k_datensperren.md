# Заблокированные данные в многопользовательском режиме

если какое-либо свойство объекта изменяется, все [свойства](Glossary_o_eigenschaften.md) блокируются. При этом "Блокирование" всегда обозначает блокирование от внесения изменений, считывание свойств другими пользователями разрешается. После обработки объект сразу же разблокируется.

### Обработка страниц и пространств листа

Только если происходит обработка страницы, пространства листа или объекта на ней, сама страница или [пространство листа](Glossary_o_bauraum.md) и все [функции](Glossary_o_funktionen.md), которые размещены на ней, блокируются для второго пользователя.

Если в многопользовательском режиме пользователь открывает вторым страницу или пространство листа, уже открытые другим пользователем, они не открываются. Если один пользователь, например, обрабатывает свойства страницы или пространства листа, то второй пользователь также может открыть эту страницу или пространство листа. Только если второй пользователь хочет, например, обрабатывать свойства [устройства](Glossary_o_betriebsmittel.md) или также свойства страницы/пространства листа, системой EPLAN будет вызвано диалоговое окно [Многопользовательский конфликт](xesmultiuserconflictgui_d_konflikte.md).

### Обработка функций в навигаторах

если Вы изменяете функцию в навигаторе, то во время обработки она будет заблокирована. если функция размещена, то при этом также будут заблокированы страница и все находящиеся на ней [объекты](Glossary_o_objekte.md). Вышестоящие и нижестоящие [узлы](Glossary_o_baugruppen.md) функций в представлении в виде дерева не блокируются, может быть так, что соответствующие функции размещены на той же странице, что и измененная функция.

!!! example "Пример:"

    Так, один пользователь может переименовать определение клеммниковX1, размещенное на странице8в навигаторе устройствX5, в то время как другой пользователь обрабатывает отдельную клемму этого клеммника, которая размещена на странице6. В таком случае будет переименовано только определение клеммников, а не все функции клеммника!если же необходимо переименовать весь клеммник, то нужно разместить курсор на том узле дерева, который представляет устройствоX1. Затем пользователь выбирает во всплывающем меню пункт меню Переименоватьи задает новый ОУX5. Во время этого процесса все функции с этим ОУ заблокированы для другого пользователя.

**См. также:**

* [Конфликт между пользователями](xesmultiuserconflictgui_k_start.md)
С вашей помощью мы можем улучшить работу системы. Мы документируем ваши действия в Google Analytics, чтобы постоянно совершенствовать справочную систему ([Дополнительная информация и возможности подачи возражений](helpsystem_hinweise_optout.md)).

Скрыть сообщение
