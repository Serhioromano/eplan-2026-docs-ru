# Вкладки Гидравлика / Пневматика / Охлаждение / Смазка / Смазочно-охлаждающая жидкость / Газовая техника / Технология производственных процессов

Вы открыли проект. Параметры > Настройки > Проекты > "Имя проекта" > Соединения > Свойства. Выберите вкладку Гидравлика, Пневматика, Охлаждение, Смазка, Смазочно-охлаждающая жидкость, Газовая техника или Технология производственных процессов.

На данной вкладке определите предварительные настройки для свойств гидротехнических соединений всего проекта. Данные свойства по умолчанию присваиваются всем соединениям проекта. Однако они могут быть перезаписаны отдельными соединениями.

!!! note "Замечание:"

    Вкладки Гидравлика, Пневматика, Охлаждение, Смазка, Смазочно-охлаждающая жидкость и Газовая техника отображаются только при наличии Fluid-лицензии.

Обзор основных элементов диалогового окна:

Групповое поле "Поперечное сечение/диаметр"

### Значение:Единица измерения

Введите здесь значение по умолчанию для поперечного сечения или диаметра соединений, а также для соответствующей единицы измерения.

Обзор возможных единиц измерения приводится в соответствующем разделе "[Соединения: Единицы измерения длины и поперечного сечения](connectionbrowsergui_k_einheitenverbindungen.md)".

### Альтернативное значение:Альтернативная единица

Здесь можно задать альтернативное поперечное сечение / диаметр, а также соответствующую альтернативную единицу измерения.

Альтернативные свойства независимы от базы данных изделий, должны заполняться вручную, отчет также должен генерироваться вручную. С помощью этих свойств поперечное сечение / диаметр соединения можно указать одновременно в различных единицах (например, AWG и мм²). Свойства доступны в диалоговом окне свойств для соединений и могут использоваться в отчетах.

* * *

### Длина

Введите здесь значение по умолчанию в метрах для длины соединения.

### Единица измерения длины

Раскрывающийся список предлагает на выбор возможные единицы измерения длины соединения. Обзор возможных единиц измерения приводится в соответствующем разделе "[Соединения: Единицы измерения длины и поперечного сечения](connectionbrowsergui_k_einheitenverbindungen.md)".

### Обозначение типа

Укажите здесь обозначение типа изделия соединения (например, номер типа трубы или шланга). Значение переносится в свойство Соединение: Обозначение типа и служит в качестве значения по умолчанию для новых соединений.
При выборе изделия значение из базы данных изделий, заданное на вкладке Данные соединения, перемещается в свойство и заменяет значение по умолчанию.

**См. также:**

* [Предварительно определить свойства соединения](connectionbrowsergui_h_verbindungseigenschaftendefinieren.md)
* [Вкладка Электротехника](connectionsettingsgui_r_projekteinstellungenelektrotechnik.md)
