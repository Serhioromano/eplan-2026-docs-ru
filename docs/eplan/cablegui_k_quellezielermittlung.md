# Определение источника и цели

Определение источника и цели кабеля осуществляется по следующим правилам:

* В первую очередь учитываются идентифицирующие иерархические данные из структурного идентификатора.
* Если оба конца являются клеммами или штекерами, а буквенное обозначение идентично, то источником будет меньший числитель.

!!! example "Пример:"

    `X1`находится перед`X4`, т. е. источником является`X1`.`X4`находится перед`X33`, т. е. источником является`X4`.

* Если оба конца являются клеммами или штекерами, а буквенные обозначения не совпадают и не равны "Х", источником будет первое по алфавиту буквенное обозначение.

!!! example "Пример:"

    `A44`находится перед`B34`, т. е. источником является`A44`.

* Если оба конца являются клеммами или штекерами, если буквенные обозначения не совпадают и одно из них равно "Х", целью всегда будет буквенное обозначение "Х".

!!! example "Пример:"

    При`Y4`и`X33`источником будет`Y4`, а целью —`X33`. При`A4`и`X33`источником будет`A4`, а целью —`X33`.

* Если оба конца не являются клеммами или штекерами, источником будет первое по алфавиту буквенное обозначение.

!!! example "Пример:"

    `M34`находится перед`M4`, т. е. источником является`M34`.`L4`находится перед`M34`, т. е. источником является`L4`.

* Если клеммы или штекеры имеются только на одном конце соединения, а на другом отсутствуют, источником всегда будет конец с клеммами или штекерами. Это правило действительно вне зависимости от идентифицирующих иерархических данных (правило 1).
* Если кабели имеют несколько целей, предыдущие правила распространяются на (графически) первое кабельное соединение.
* Если ОУ изменяется только на одном конце соединения, источник и цель кабеля сохраняются.
* Если ОУ изменяется по обоим концам одновременно, источник и цель еще раз устанавливаются по предыдущим правилам.

### Сортировка кабельных соединений

Кабельные соединения сортируются при автоматическом присвоении соединений изделия. Сортировка определяет последовательность присвоения кабельных соединений:

* Если кабель имеет только источник, то кабельные соединения сортируются в той последовательности, в которой они находятся в схеме соединений. Если источником является клеммник, то кабельные соединения сортируются в соответствии с сортировкой клемм.
* Если кабель имеет несколько источников, то кабельные соединения сначала сортируются в группы по источникам. Для определения последовательности источников используются вышеперечисленные правила определения источника и цели.

Исключение: если цель — штекер, источник — не штекер, а в диалоговом окне **Настройки: Кабели** установлен флажок **Кабельное соединение всегда сортировать по последовательности штекеров**, сортировка будет осуществляться в последовательности на цели.

**См. также:**

* [Кабели](cablegui_k_start.md)
* [Определение кабелей](cablegui_k_arbeitsweise.md)
