## Настройка отображения таблиц

В некоторых табличных представлениях, например в диалоговом окне Управление структ. идентификаторами или в диалоговом окне Конфигурировать свойства для условных обозначений, при обработке клеммников, штекеров и т. п. можно вручную изменять и оптимизировать порядок отображения и сортировки объектов.

Кроме того, во всех таблицах можно регулировать ширину столбца.

!!! note "Замечание:"

Условие:

Вы открыли диалоговое окно, в котором содержится табличное представление.

### Сортировка объектов таблиц

В некоторых табличных представлениях, например в диалоговых окнах Управление сообщениями или Управление слоями, при табличной обработке в диалоговом окне Табличная обработка и т. д. можно сортировать объекты в возрастающем или убывающем алфавитно-цифровом порядке.

1. Щелкните двойным щелчком на заголовке одного из столбцов.  

Если отображенные данные представлены в своей первоначальной последовательности, объекты в выбранном столбце будут сначала сортироваться в алфавитно-цифровом восходящем порядке. (Стрелка в заголовке столбца  при этом указывает на направление сортировки.)
2. Щелкните еще раз на заголовке столбца двойным щелчком.  

Сортировка будет выполнена в алфавитно-цифровом нисходящем порядке, в зависимости от столбца, на заголовке которого вы щелкнули. (Стрелка в заголовке столбца  при этом указывает на направление сортировки.)
3. Третий раз щелкните двойным щелчком на заголовке столбца.  

Данные в столбце будут снова отображены в своей первоначальной последовательности.

### Оптимизировать отображение отдельных столбцов

Для оптимального отображения столбцов, т. е. оптимальной ширины, необходимо выполнить следующие действия:

1. Щелкните два раза по разделителю столбцов справа рядом с заголовком столбца, ширину которого вы желаете оптимизировать.  

Ширина столбца оптимизируется также, как и заголовок, и содержимое столбцов становится видно полностью.  

Если включена опция Ширина столбцов автоматическая, он будет деактивирован.

### Оптимизировать отображение всех столбцов

Для оптимальной настройки ширины всех столбцов выполните следующее:

1. Выберите пункт всплывающего меню Подобрать ширину столбца.  

Ширина всех столбцов регулируется также, как и заголовок, и содержимое столбцов становится видно полностью.  

Если включена опция Ширина столбцов автоматическая, он будет деактивирован.
2. Или выберите пункт всплывающего меню Автоматическая ширина столбцов.  

Ширина столбцов регулируется так, чтобы таблица отображалась полностью во всей доступной области.  

Флажок перед пунктом всплывающего меню Ширина столбцов автоматическая показывает, что активирована автоматическая ширина столбцов. Повторный выбор деактивирует эту опцию.

!!! tip "Совет:"

    Вы можете назначить собственные сочетания клавиш для командПодобрать ширину столбцаиШирина столбцов автоматическая. Чтобы эти команды отображались в полеКомандыв диалоговом окнеНастроить комбинации клавиш, необходимо выбрать запись "Все команды" в полеВкладки. Заданные таким образом комбинации клавиш можно использовать в диалоговых окнах, которые могут оставаться открытыми во время обработки проекта, например в управлении сообщениями, в табличной обработке или в представлениях в виде списка навигаторов.

### Настройка ширины нескольких столбцов одновременно

1. В таблице выделите столбцы, ширину которых следует настроить.
2. Для одного из столбцов нажмите на разделитель столбцов у правого края, удерживайте левую кнопку мыши и перетащите столбец на нужную ширину.  

Ширины остальных столбцов настраивается соответствующим образом.

### Оптимизация отображения отдельных строк

В некоторых диалоговых окнах, содержащих таблицу (например, первая вкладка диалогового окна свойств или вкладка Свойства в базе данных изделий) можно индивидуально отрегулировать высоту строк.

1. В нужной строке выберите пункт всплывающего меню Настроить строку.
2. В диалоговом окне Настроить строку в поле Высота строки установите желаемую высоту выделенной строки.
3. При необходимости активируйте пункт Полужирный шрифт, чтобы выделить жирным шрифтом текст выбранной строки.
4. Щелкните по кнопке [OK].  

Выполненные настройки будут сохранены для выбранной строки.

!!! note "Замечание:"

    В диалоговых окнахОбработать клеммник,Обработать штекериМногоязычный вводнастройка высоты строки влияет навсестроки таблицы. Возможность выделить текст жирным шрифтом отсутствует.В диалоговом окнеСловарьвысота строки фиксирована и составляет три строки.

### Изменить размер шрифта с помощью колесика мыши

Во многих табличных представлениях, например в диалоговых окнах Управление сообщениями или Управление слоями, при табличной обработке в диалоговом окне Табличная обработка и т. д. можно изменять отображаемый размер шрифта, вращая колесико мыши при нажатой клавише [Ctrl].

1. Щелкните по таблице.
2. Удерживайте клавишу [Ctrl] и прокрутите колесико мыши вверх или вниз.  

Размер шрифта в таблице увеличивается или уменьшается.
3. Выберите пункт всплывающего меню Размер шрифта 100 %.  

Тексты в таблице снова отображаются с исходным размером шрифта.

