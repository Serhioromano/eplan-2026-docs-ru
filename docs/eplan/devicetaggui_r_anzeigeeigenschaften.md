


[](EPLAN_Help_k_start.htm)

  * placeholder



  * Все файлы






Эта функциональность предусмотрена только в определенных модулях расширения. [Информация / авторское право](license_k_start.htm)

Вы находитесь здесь:

## Свойства отображения: Размещенное свойство

[Вызов диалогового окна:](javascript:void\(0\);)

Вы открыли проект. Вы открыли диалоговое окно, в котором можно обрабатывать свойства отображения размещенных свойств, например диалоговое окно "Свойства" для условного обозначения.

  * [Вкладка Отображение (Устройство)](devicetaggui_r_anzeige.htm)
  * [Вкладка Отображение (Точка определения контура)](contoureditorgui_r_anzeige.htm)
  * [Вкладка Отображение (обзор модели)](gededit3dgui_r_anzeige.htm)
  * [Диалоговое окно Порядок свойств](eservicesgui_d_eigenschaftsanordnung.htm)
  * [Диалоговое окно Вывод устройства](symboleditorgui_d_anschlusspunkt.htm)
  * [Диалоговое окно Размещение свойства](symboleditorgui_d_eigenschaftsplatzierung.htm)



В таблице Свойство / Присвоение вы обрабатываете свойства отображения размещенного свойства. Здесь доступны уровни иерархии Формат, Рамка текста, Блок выравнивания, Значение / единица измерения, Позиция и Дата / время, открыть которые можно, щелкнув по символу .

Обзор основных элементов диалогового окна:

[Уровень иерархии Формат](javascript:void\(0\);)

Выравнивание:

Выберите из раскрывающегося списка требуемое выравнивание текста. Выравнивание указывает, где находится точка ввода относительно текста. Эта настройка может быть выполнена только для основных свойств и действует для всех свойств блока (т. е. на основное свойство и присоединенные свойства).

1: Вверху слева 2: В центре слева 3: База слева 4: Внизу слева |  |  9: Вверху справа 10: В центре справа 11: База справа 12: Внизу справа  
---|---|---  
|  5: Вверху по центру 6: В центре по центру 7: База по центру 8: Внизу по центру |   
  
Угол:

Эта настройка может быть выполнена только для основных свойств и действует для всех свойств блока (т. е. на основное свойство и присоединенные свойства).

Межстрочный интервал:

Данное поле позволяет задать вертикальный интервал между строками текста, т. е. расстояние от нижнего края одной строки до нижнего края другой. Интервал между строками зависит от выбранного вида шрифта. Выберите запись из раскрывающегося списка или введите значение вручную. При этом значение '1' соответствует одинарному интервалу между строками, значение '2' —двойному интервалу между строками и т. д. Одинарный интервал между строками составляет 1,9-кратный размер шрифта (высота прописных букв 'M').

Язык:

Через этот раскрывающийся список вы указываете, на каком языке должен отображаться текст свойства.

  * Все языки отображения (друг под другом): переведенный текст отображается на всех языках, выбранных в специфических для проекта настройках перевода в поле Языки отображения (Файл > Настройки > Проекты > "Имя проекта" > Перевод > Общее). Тексты переводов позиционируются на страницах проекта друг под другом.
  * Все языки отображения (рядом друг с другом): переведенный текст отображается на всех языках, выбранных в специфических для проекта настройках перевода в поле Языки отображения (Файл > Настройки > Проекты > "Имя проекта" > Перевод > Общее). Тексты переводов позиционируются на страницах проекта рядом друг с другом.

!!! note "Замечание:"

    Если на уровне иерархииРамкаактивирован блок выравнивания, то данная настройка имеет преимущество. Это может привести к тому, чтоВсе языки отображения (рядом друг с другом)иВсе языки отображения (друг под другом)не показывают различия.



  * Одноязычн. (перемен.): переведенный текст отображается на всех языках, выбранных в специфических для проекта настройках перевода в поле (Файл > Настройки > Проекты > "Имя проекта" > Перевод > Общее).
  * Без привязки к языку: эта настройка может использоваться для текстов, которые внесены без привязки к тексту (например, как числовые значения) и не требуют перевода. Если случайно этот текст будет переведен, после этого отобразится пустая запись или текст для отсутствующего перевода (если этот параметр активирован в настройках перевода).
  * <Язык>: Переведенный текст выводится на настроенном здесь языке независимо от того, какой исходный язык выбран для данного проекта.



Интервал между абзацами:

Здесь задается вертикальный интервал между двумя присоединенными свойствами, расположенными при отображении в одной общей группе. Выберите значение из раскрывающегося списка или введите десятичное значение вручную.



!!! note "Замечание:"

    Учтите, пожалуйста, что при присоединенных текстах настроенный интервал между строками добавляется к интервалу между абзацами.


 

[Уровень иерархии Рамка текста](javascript:void\(0\);)

Размер из настройки проекта:

Если этот флажок установлен, размер рамки текста берется из одноименной специфической для проекта настройки (Файл > Настройки > Проект > "Имя проекта" > Графическая обработка > Общее).

Если этот флажок снят, размер рамки зависит от текста.



!!! note "Замечание:"

    Рамка текста типа "Овал" обозначается так же, как и прямоугольник, грани которого расширяются двумя полукругами. В случае с этой рамкой текста ее размер, заданный в настройках проекта, будет соответствовать исключительно взятому за основу прямоугольнику. Боковые полукруги не учитываются. Таким образом, овал имеет большую ширину, чем прямоугольник.


 

Обновить указательную линию:

Если этот флажок установлен, от текста к соответствующей функции будет проведена указательная линия.

Обратите внимание, что при [перемещении текстов свойств](gededitgui_h_symboltexteverschieben.htm) с помощью команды Переместить будут перемещены и указательные линии (командный путь: вкладка Главная > группа команд Текст > Переместить).

[Уровень иерархии Блок выравнивания](javascript:void\(0\);)

Ширина текста фиксирована:

Если этот флажок установлен, текст подгоняется по ширине рамки. Т.е. ширина текста не превысит ширины рамки. Если текст слишком длинный, он расширяется вниз (или вверх, в зависимости от расположения рамки), а между словами появляются разрывы. Если слово длиннее ширины рамки, разрыв выполняется по центру слова.

Высота текста фиксирована:

Если этот флажок установлен, текст подгоняется по высоте рамки. Т.е. высота текста не превысит высоты рамки. Если текст слишком длинный, строки удлиняются вправо (или влево, в зависимости от способа выравнивания рамок). Определенные пользователем разрывы строк заменяются пробелами. Все пробелы перед переходом на новую строку убираются, а пробелы после перехода на новую строку остаются.

Убрать переходы на новую строку:

Если этот флажок установлен, определенные пользователем разрывы строк заменяются пробелами. Все пробелы перед переходом на новую строку убираются, а пробелы после перехода на новую строку остаются.

Никогда не разделять слов:

Если этот флажок установлен, слова, длина которых превышает ширину/высоту рамки, не разделяются, а выходят за пределы рамки.



!!! note "Замечание:"

    При использовании настройкиВыравнивание(через свойствоФормат) одновременно выравнивается и блок выравнивания. При использовании настройкиУгол(также через свойствоФормат) одновременно поворачивается и блок выравнивания. Если блок выравнивания содержит заполнитель для графического символа, он поворачивается на указанный угол, при этом можно использовать любой угол.


 

Разрешен подгон текста:

Если этот флажок установлен, для этого текста разрешается автоматический подгон под блок выравнивания. Условием является то, что, кроме блока выравнивания, активируются также обе настройки Ширина текста фиксирована и Высота текста фиксирована. Тексты подгоняются с помощью команды [Подогнать тексты в блоке выравнивания](gededitgui_h_texteeinpassen.htm). Для текстов-заполнителей в формах при генерировании отчетов подгон выполняется автоматически.



!!! example "Пример:"

    На следующих рисунках показано действие различных настроек на текст в блоке выравнивания. Выводимый текст содержит разрыв строки.Флажок не установлен:АктивированаШирина текста фиксирована:АктивированыШирина текста фиксированаиНикогда не разделять слов:АктивированыШирина текста фиксированаиУбрать переходы на новую строку:АктивированыШирина текста фиксирована,Никогда не разделять словиУбрать переходы на новую строку:АктивированаВысота текста фиксирована(с маленьким блоком выравнивания):АктивированыШирина текста фиксирована,Высота текста фиксирована,Разрешен подгон текста(с маленьким блоком выравнивания):


 

Представление:

Доступно только при активированном блоке выравнивания. С помощью этого раскрывающегося списка для размещенного свойства можно указать, должно ли значение свойства отображаться в виде QR-кода или изображения вместо представления в виде текста.

Настройка "QR-код" целесообразна, например, для свойств изделий Внешний документ: Файл / гиперссылка [1–20], чтобы отобразить гиперссылку внешнего документа на присвоенном изделии в виде QR-кода. Настройка "Графический файл" целесообразна, например, для свойства изделий Графический файл, чтобы отобразить графический файл присвоенного изделия на условном обозначении в графическом редакторе. При этом размер блока выравнивания определяет размер изображения или QR-кода.

Если нужное представление (например "Графический файл") невозможно для какого-либо свойства в порядке свойств, ничего не отображается.

[Уровень иерархии Значение / единица измерения](javascript:void\(0\);)

Отображение единицы измерения:

Для данного свойства по умолчанию настроено "без изменений". При этой настройке свойство отображается так же, как оно было задано в соответствующем поле. Другие настройки на уровне иерархии Значение / единица измерения в этом случае недоступны.

Чтобы выбрать другое отображение единицы измерения, нажмите на столбец Присвоение, а затем [...]. Откроется алфавитный список физических величин (например, "Давление", "Длина", "Масса" и т. д.) в виде дерева с иерархической структурой. Щелкните символ , чтобы открыть требуемый уровень иерархии определенного размера (например, "Мощность"). Двойным щелчком мыши выберите в находящемся ниже уровне иерархии одну из приведенных единиц измерения (например, "МВт"). После этого будут активированы другие свойства отображения на уровне иерархии Значение / единица измерения. Все перечисленные для данного размера единицы измерения обозначены здесь как "Группа единиц измерения".

Основные единицы измерения выделены в списке жирным шрифтом. Отображенные здесь размеры и единицы измерения управляются Eplan, и вы не можете их изменять или расширять. При определенных размерах ("Длина", "Масса") вы можете применять единицы отображения из настроек проекта или пользовательских настроек.



!!! note "Замечание:"

    Обратите внимание, что все единицы измерения, не относящиеся к Международной системе единиц (СИ) (т. е., например, "миля", "фут", "унция" и т. д.), основываются на американской системе единиц.


 

Переменное число десят. разрядов:

Установите данный флажок, если количество цифр после запятой не должно заполняться нулями; однако число всегда округляется до количества цифр, указанного в поле Десятичные разряды.



!!! note "Замечание:"

    


 

!!! example "Пример:"

    В полеТехнические параметры, к примеру, выводится следующее:Мощность A: 750 Вт, Мощность B: 500 ВтНа вкладкеОтображениедля данного свойства на уровне иерархииЗначение / единица измеренияустановлена единица отображениякВт, а в полеДесятичные разряды— значение3. После того как вы щелкните[Применить], введенные данные в полеТехнические параметрыостанутся неизменными, а в графическом редакторе отобразится следующий текст:Мощность A: 0,750 кВт, Мощность B: 0,500 кВтЕсли в качестве единицы отображения установленомВ, то программа предполагает, что ввод осуществляется в основных единицах измеренияВи пересчитывает их вмВ. В таком случае отображаются обе единицы измерения. Отображение в графическом редакторе выглядит следующим образом:Мощность A: 750000 мВ Вт, Мощность B: 500000 мВ Вт


 

Ноль как знак пробела:

Доступно только для свойств с числовыми значениями. Если этот флажок установлен, значение "0" заменяется пробелом.

Такая настройка может пригодиться, например, для порядков свойств или текстов-заполнителей при их использовании в таблицах, в которых не нужно отображать ноль. К примеру, в предварительном планировании это актуально для графического представления схем автоматизации или вывода списков функций для [автоматизации зданий](planninggui_k_gebaeudeautomation.htm).

[Уровень иерархии Позиция](javascript:void\(0\);)

Присоединение по умолчанию:

Данный раскрывающийся список позволяет задать для основного свойства значение по умолчанию для присоединения подчиненных свойств. Эта настройка действует для всех свойств блока (т. е. на основное свойство и присоединенные свойства). Пиктограмма рядом с основным свойством отображает выбранное присоединение по умолчанию.

Пиктограмма |  Присоединение по умолчанию |  Значение  
---|---|---  
|  Внизу |  Если возможно, подчиненное свойство присоединяется под основным свойством.  
|  Вверху |  Если возможно, подчиненное свойство присоединяется над основным свойством.  
|  Справа |  Если возможно, подчиненное свойство присоединяется справа от основного свойства.  
|  Слева |  Если возможно, подчиненное свойство присоединяется слева от основного свойства.  
  
По центру блока:

Данный раскрывающийся список позволяет задать для основного свойства необходимость центрирования блока (т. е. основного свойства и присоединенных свойств). Блок центрируется относительно исходной точки, заданной для основного свойства. При этом учитываются только видимые тексты свойств, тогда как пустые и невидимые тексты свойств игнорируются. При центрировании учитываются также интервалы между абзацами. Интервал после последнего текста свойства игнорируется.

  * Центрирование блока отсутствует: Блок с текстами свойств не центрируется.
  * Все тексты свойств: Центрируется весь блок с текстами свойств, т. е. центр блока расположен в исходной точке.
  * Первый текст свойства: Центрируется только первый видимый текст свойства, и к нему добавляются остальные тексты свойств. Центр первого текста свойства находится в исходной точке.
  * Первые два текста свойств: Центрируются два первых видимых текста свойств, а к ним добавляются остальные тексты свойств. Центр первых двух текстов свойств находится в исходной точке.
  * Первые три текста свойств: Центрируются три первых видимых текста свойств, а к ним добавляются остальные тексты свойств. Центр первых трех текстов свойств находится в исходной точке.



!!! example "Пример:"

    На одном условном обозначении отображаются пять текстов свойств. ПараметрПрисоединение по умолчаниюустановлен на 'Снизу'.Исходная точкадля размещения основного свойства — это точка вставки условного обозначения со смещением по оси Х на '-8 мм'. ПараметрВыравниваниеустановлен на 'Вверху справа', т. е. при настройке 'Центрирование блока отсутствует' в исходной точке находится правый верхний угол рамки текста. Для второго и третьего текстов свойствИнтервал между абзацаминастроен на '1,8 мм'.При выбореопцииВсе тексты свойствблок располагается таким образом, чтобы в исходной точке находиласьсередина блока. Если общая высота блока составляет "x", то после центрирования высота над исходной точкой и под ней составит "x/2".При выборе опцииПервый текст свойстваблок располагается таким образом, чтобы в исходной точке находиласьсередина первого видимого текста свойства.При выборе опцииПервые два текста свойствблок располагается таким образом, чтобы в исходной точке находиласьсередина первых двух видимых текстов свойств.При выборе опцииПервые три текста свойствблок располагается таким образом, чтобы в исходной точке находиласьсередина первых трех видимых текстов свойств.Если для присоединения по умолчанию задано значение не "Снизу", а "Справа", то при центрировании с опцией "Все тексты свойств" блок располагается таким образом, чтобы части блока справа и слева от исходной точки были одинаковы.


 

Исходная точка:

В этом поле устанавливается исходная точка для координат размещенного основного свойства (и присоединенных к нему вспомогательных свойств). По умолчанию устанавливается точка вставки условного обозначения или объекта. Черные ящики, блоки ПЛК, линии определения кабелей и т. д. можно дополнительно выбрать в раскрывающемся списке между всеми точками элементов соответствующего объекта, кроме того, возможно выравнивание по центру.

Исходной точкой для координат размещенного свойства вывода устройства является сам вывод устройства.

При обработке выводов символов в редакторе символов эта настройка недоступна.

Абсцисса / ордината:

Данные координат в этих полях перемещают позицию размещенного основного свойства относительно выбранной выше исходной точки.

При активации одной или даже двух настроек Координата X автоматически (зона) или Координата Y автоматически (зона) данные, введенные в полях Координата X и Координата Y, основываются на исходной точке, уже получившей смещение от автоматической координаты.

Координата X автоматически (зона) / Координата Y автоматически (зона):

Настройка Координата Y автоматически (зона) позволяет отображать размещенные свойства условного обозначения по зонам, под чертежом. Если флажок установлен, то при размещении текста свойств учитывается только координата X исходной точки, в то время как координата Y задается автоматически. Значение для координаты Y (по отношению к нулевой точке страницы) задайте в свойстве Порядок свойств: Координата Y автоматически (зона) (Ид. 12062). Это свойство можно задать как на рамке, так и на странице. Если для свойства на странице введено значение, оно перезаписывает настройку на рамке.

Настройка Координата X автоматически (зона) позволяет отображать размещенные свойства условного обозначения по строкам, рядом с чертежом. Эта настройка важна, например, при выполнении проектов по стандарту NFPA. Если флажок установлен, то при размещении текста свойства учитывается только координата Y исходной точки, в то время как координата X задается автоматически. Значение для координаты X (по отношению к нулевой точке страницы) задайте в свойстве Порядок свойств: Координата X автоматически (зона) (Ид. 12063). Это свойство можно задать как на рамке, так и на странице. Если для свойства на странице введено значение, оно перезаписывает настройку на рамке.



!!! tip "Совет:"

    Чтобы с помощью автоматической координаты Y упорядочить в зоне несколько текстов свойства в формесписка, введите в полеКоордината Yразные значения для каждого свойства. Тексты свойства смещаются относительно автоматической координаты Y.Чтобы упорядочить тексты свойства в форметаблицы, введите разные значения в поляхКоордината XиКоордината Y.


 
