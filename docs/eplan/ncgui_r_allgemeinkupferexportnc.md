# Вкладка Общие (Настройки: Экспорт ЧУ — медь)

* Файл > Настройки > Компания > Механическая обработка > Экспорт ЧУ — медь. Выберите вкладку Общее.
* Файл > Экспортировать > группа команд Данные изготовления > Механическая обработка > ЧУ — медь. Нажмите рядом с полем Машина на кнопку ++...++. Выберите вкладку Общ..

На этой вкладке установите общие настройки для экспорта ЧУ медных функциональных элементов.

Обзор основных элементов диалогового окна:

### Каталог вывода:

Экспорт ЧУ — медь будет сгенерирован в виде файла XML. В этом поле укажите место хранения. В качестве целевых для экспорта данных можно указать любой дисковод и подкаталог.

С помощью пункта всплывающего меню Вставить переменную пути перейдите в диалоговое окно [Выбрать переменную пути](modaldialogsdb_d_pfadvariablen.md), из которого можно будет скопировать одну из доступных переменных пути.

### Генерировать имена файлов:

В этом раскрывающемся списке можно задать стандартный порядок присвоения имен файлам при экспорте ЧУ — медь:

* Из непрерывного номера: при выборе этого параметра Eplan по умолчанию присваивает файлам XML имена по образцу <непрерывный_номер>.XML. Если уже имеются файлы XML, Eplan начнет нумерацию со следующего доступного числа.
* Из обозначения функц. элемента: При выборе этого параметра Eplan по умолчанию присваивает файлам XML имена по образцу <Обозначение функц. элемента>.XML.
* Из свойств: если выбран этот параметр, имя файла составляется из свойств проекта, а также свободно определяемого разделителя. Используя кнопку ++...++, откройте диалоговое окно Формат: Свойство для конфигурации имени файла.

### Генерировать подкаталоги:

В этом раскрывающемся списке можно задать стандартный порядок создания подкаталогов при экспорте ЧУ — медь:

* Нет: Если выбран данный параметр, при экспорте подкаталоги не генерируются.
* Из структуры проекта: Если выбран данный параметр, то при экспорте автоматически генерируются подкаталоги, отражающие структуру проекта.
* Из свойств: Если выбран этот параметр, имя подкаталога составляется из свойств проекта, а также свободно определяемых разделителей / разделителей каталога. При нажатии кнопки ++...++ открывается диалоговое окно Формат: Свойство для конфигурации имени каталога. Воспользуйтесь, например, элементом формата "Разделитель каталога", чтобы создать структуру каталога с несколькими подкаталогами, расположенными друг под другом.

