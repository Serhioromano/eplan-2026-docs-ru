## Присвоить кабельные соединения автоматически

На вкладке Соединения ленты у вас есть две разные возможности автоматически присвоить кабельным соединениям соединения изделия кабеля:

* Команда Все присвоить заново перезаписывает все свойства соединений свойствами из изделия. Сравнивается только тип потенциала.
* Команда Оставить имеющиеся свойства без изменения оставляет имеющиеся свойства соединения без изменений. Присвоение происходит только в том случае, если все идентифицирующие свойства кабельного соединения соответствуют свойствам соединения (изделия).

При этом для выделенных кабелей повторно сортируется последовательность кабельных соединений. [Сортировка кабельных соединений](cablegui_k_quellezielermittlung.md#I_Sortierung) определяет последовательность присвоения кабельных соединений. Сортировка основана на источнике кабеля, т. е. устройства источника определяют порядок кабельных соединений. Если устройство представляет собой клеммник, порядок соединений определяется в соответствии с порядком расположения клемм на клеммнике.

!!! tip "Совет:"

    В диалоговом окне навигатораКабель - <имя проекта>и в диалоговом окнеОбработать кабельдополнительно доступны командыВсе присвоить зановоиОставить имеющиеся свойства без измененияпод пунктом всплывающего менюПрисвоить кабельные соединения.

Условия:

* Вы выделили линию определения кабеля в Графическом редакторе или определение кабеля в диалоговом окне навигатора Кабели — <Имя проекта>.
* Кабелю присвоено изделие.

### Присвоить все кабельные соединения заново

1. Выберите следующие команды: Вкладка Соединения > группа команд Кабель > Все присвоить заново.  
  
Если тип потенциала совпадает, соединения изделия кабеля присваиваются кабельным соединениям. Существующие свойства кабельных соединений перезаписываются со свойств изделия.  
  
Данные точек определения соединений соответственно обновляются. Имеющиеся данные перезаписываются. Новые данные записываются на точку определения соединения, только если они не записаны на соответствующей линии определения кабеля / экранировании.  
  
Порядок экранирований сохраняется, но обозначения экранирований могут изменяться.

### Присвоить кабельные соединения с подходящими свойствами

Идентифицирующие свойства присвоения:

* Определение функции
* Обозначение экранирования (Экранировано от)
* Парный индекс
* Тип потенциала
* Цвет / номер
* Поперечное сечение / диаметр.

Правила присвоения:

* Если свойство соединения изделия заполнено, кабельное соединение должно иметь аналогичное значение.
* Пустые свойства соединения изделия подходят к любым значениям кабельного соединения.
* Экранированные жилы подходят и к неэкранированным соединениям; но если подходящее экранированное соединение уже найдено, то его присвоение осуществляется в первую очередь.

1. Выберите следующие команды: Вкладка Соединения > группа команд Кабель > раскрывающаяся кнопка Все присвоить заново > Оставить имеющиеся свойства без изменения.  
  
Соединения изделия кабеля присваиваются кабельным соединениям, если их идентифицирующие свойства совпадают.  
  
Имеющиеся свойства кабельных соединений остаются при этом без изменений, дополнительные свойства изделия кабеля дополняются.

!!! example "Пример:"

    СвойствоПоперечное сечение / диаметримеет следующие значения для соединения изделия и кабельного соединения:Соединение изделияКабельное соединениеПодходящие5 мм²5 мм²да5 мм²2 мм²нет5 мм²–нет–5 мм²да–2 мм²да

**См. также:**

* [Определение источника и цели](cablegui_k_quellezielermittlung.md)
